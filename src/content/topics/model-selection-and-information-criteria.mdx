---
title: "Model Selection & Information Criteria"
subtitle: "When every candidate model fits the training data differently, a principled ranking criterion is the only honest arbiter. AIC (Akaike) estimates out-of-sample log-likelihood asymptotically; BIC (Schwarz) approximates the Bayesian marginal likelihood; Mallows' Cp is the Gaussian special case; Stone's 1977 theorem identifies LOO-CV with AIC; Yang 2005 proves that selection consistency and prediction efficiency are mutually incompatible. Track 6 closes here."
status: "published"
difficulty: "intermediate"
prerequisites:
  - "linear-regression"
  - "generalized-linear-models"
  - "regularization-and-penalized-estimation"
  - "maximum-likelihood"
  - "likelihood-ratio-tests-and-np"
  - "point-estimation"
  - "confidence-intervals-and-duality"
  - "exponential-families"
tags:
  - "statistics"
  - "model-selection"
  - "information-criteria"
  - "aic"
  - "bic"
  - "mallows-cp"
  - "aicc"
  - "tic"
  - "cross-validation"
  - "stone-theorem"
  - "yang-incompatibility"
  - "consistency-vs-efficiency"
  - "laplace-approximation"
  - "marginal-likelihood"
  - "bayesian-bridge"
  - "effective-degrees-of-freedom"
  - "nested-model-selection"
  - "kullback-leibler-divergence"
domain: "regression-linear-models"
videoId: null
notebookPath: "notebooks/model-selection/24_model_selection.ipynb"
githubUrl: "https://github.com/jonx0037/formalStatistics/blob/main/src/content/topics/model-selection-and-information-criteria.mdx"
datePublished: 2026-04-26
estimatedReadTime: 60
abstract: "Track 6 opened with linear regression as orthogonal projection (Topic 21) and continued with generalized linear models as IRLS on the exponential family (Topic 22). Topic 23 added penalization to stabilize both under collinearity and separation, with cross-validation as the default selector for the penalty parameter $\\lambda$. Topic 24 closes the track by formalizing the selection problem itself: given a candidate family $\\{\\mathcal{M}_1, \\mathcal{M}_2, \\dots\\}$, which model should we use, and by what criterion? The three main answers — AIC, BIC, and cross-validation — look superficially similar ($-2\\hat\\ell + \\text{penalty}$) but rest on distinct asymptotic guarantees. The featured §24.3 gives Akaike's theorem in full: AIC estimates the expected Kullback–Leibler divergence between the fitted model and the truth, with the $+2k$ penalty derived as the bias-correction term in the plug-in estimate of expected log-likelihood. §24.4 develops BIC as the Laplace approximation to the Bayesian marginal likelihood, with the $k\\log n$ penalty emerging from the second-order expansion — opening the on-ramp to Track 7's Bayesian machinery. §24.5 proves Stone's 1977 equivalence: LOO-CV and AIC select the same model asymptotically under Gaussian-homoscedastic errors, closing the loop on Topic 23's $\\lambda$-selection apparatus. §24.6 states Yang's 2005 incompatibility: no procedure can be both selection-consistent (BIC, when the true model is in the candidate set) and minimax-rate optimal for prediction (AIC, CV, in the misspecified-nonparametric regime). §24.7 connects LRT-based nested-model testing (Topics 18, 21, 22) to AIC via the identity $\\Delta\\mathrm{AIC} = 2q - 2\\Delta\\hat\\ell$. §24.8 extends the framework to penalized estimators via effective degrees of freedom ($\\operatorname{tr}(\\mathbf{H}_\\lambda)$ for ridge; $\\mathbb{E}[|\\mathcal{A}|]$ for lasso), discharging Topic 23 §23.8 Rem 20. §24.9 runs three workflows — polynomial-degree on simulated data, nested Poisson GLM, lasso-path AIC on prostate-cancer. §24.10 forward-maps to Track 7 (BIC → marginal likelihood → BMA → MCMC → DIC/WAIC/PSIS-LOO), Track 8 (nonparametric selection, Rademacher complexity), and formalml (post-selection inference, debiased lasso, cross-fitting)."
formalcalculusPrereqs:
  - topic: "multivariable-calculus"
    site: "formalcalculus"
    relationship: "Akaike's proof (§24.3 Proof 1) Taylor-expands $\\ell(\\theta)$ around $\\theta_0$ in $p+1$ dimensions; the BIC-Laplace proof (§24.4 Proof 2) does multivariate Laplace with a Hessian determinant. Both rely on the second-order Taylor theorem with remainder."
  - topic: "matrix-calculus"
    site: "formalcalculus"
    relationship: "§24.5 Stone's hat-matrix identity uses the trace cyclicity property; §24.8 derives ridge effective DOF $\\sum_j d_j^2/(d_j^2+\\lambda)$ in SVD coordinates. Both rely on standard matrix-calculus identities from the formalCalculus track."
formalmlConnections:
  - topic: "post-selection-inference"
    site: "formalml"
    relationship: "After AIC/BIC/CV selection, naive Wald CIs are invalid because the selection event is data-dependent. Debiased lasso (Zhang–Zhang 2014), selective conditioning (Lee–Sun–Sun–Taylor 2016), and cross-fitting (Chernozhukov et al. 2018) restore validity. §24.10 Rem 20 forward-points."
  - topic: "bayesian-model-averaging"
    site: "formalml"
    relationship: "BIC approximates the log marginal likelihood of a single model. BMA weights predictions by posterior model probabilities. §24.4 Rem 7 flags; §24.10 Rem 19 forward-points; full treatment Track 7."
  - topic: "cross-fitting"
    site: "formalml"
    relationship: "Chernozhukov et al. 2018's double/debiased ML formalism uses cross-fitting for valid causal inference after ML-selected nuisance models. §24.10 Rem 20."
  - topic: "high-dimensional-regression"
    site: "formalml"
    relationship: "When $p \\gg n$, likelihood-based IC degenerates. Extended BIC (Chen–Chen 2008), stability selection (Meinshausen–Bühlmann 2010), and knockoffs (Barber–Candès 2015) extend the framework. §24.10 Rem 25."
  - topic: "structural-risk-minimization"
    site: "formalml"
    relationship: "Vapnik's SRM generalizes information criteria to function classes via VC dimension or Rademacher complexity. §24.10 Rem 26."
connections:
  - topic: "linear-regression"
    relationship: "Topic 21 established OLS, residual sum of squares, and the hat matrix $\\mathbf{H} = \\mathbf{X}(\\mathbf{X}^\\top\\mathbf{X})^{-1}\\mathbf{X}^\\top$. Topic 24 §24.2 Mallows' $C_p$ and §24.5 Stone's proof both rely on RSS and leverage $h_{ii}$; §24.8 effective-DOF for ridge is $\\operatorname{tr}(\\mathbf{H}_\\lambda)$. Topic 21 §21.10 Rem 24 forward-promised this entire framework — discharged by §24.2/§24.3/§24.4/§24.5/§24.6."
  - topic: "generalized-linear-models"
    relationship: "Topic 22 delivered IRLS, Gaussian log-likelihood, and deviance. §24.3's AIC uses the log-likelihood directly; §24.7 uses Topic 22's deviance Wilks identity to demonstrate AIC ≡ LRT for nested GLMs."
  - topic: "regularization-and-penalized-estimation"
    relationship: "Topic 23 introduced ridge / lasso, with CV as the default $\\lambda$-selector. Topic 24 §24.8 derives effective DOF for ridge as $\\operatorname{tr}(\\mathbf{H}_\\lambda) = \\sum_j d_j^2/(d_j^2+\\lambda)$, discharging §23.8 Rem 20. §24.5 Stone equivalence identifies $k$-fold CV with AIC asymptotically, discharging §23.8 Rem 19. §24.10 closing paragraph reciprocally frames Topic 23 as a special case of Topic 24's framework."
  - topic: "maximum-likelihood"
    relationship: "Topic 14 delivered MLE, Fisher information, and asymptotic normality. Akaike's proof in §24.3 rests on Topic 14: Taylor expansion of $\\hat\\ell$ around $\\theta_0$ uses MLE asymptotic normality (Topic 14 Thm 6); the bias correction $\\mathbb{E}[\\operatorname{tr}(\\hat{\\mathcal{J}}^{-1}\\hat{\\mathcal{K}})] = k$ uses the Fisher identity $\\mathcal{J} = \\mathcal{K}$ (Topic 14 Thm 3)."
  - topic: "likelihood-ratio-tests-and-np"
    relationship: "Topic 18 Thm 4 (Wilks) is the asymptotic engine for §24.7: for nested models differing by $q$ parameters, $2\\Delta\\hat\\ell \\sim \\chi^2_q$. §24.7 Thm 6 identifies AIC's preference for the full model with $2\\Delta\\hat\\ell > 2q$ — AIC is a default-threshold LRT."
  - topic: "point-estimation"
    relationship: "Topic 13's bias-variance MSE decomposition underpins §24.1 motivation and §24.3 Rem 4 on the optimism gap. §24.8's effective DOF for ridge is the bias-variance tradeoff expressed as a continuous effective-parameter count."
  - topic: "confidence-intervals-and-duality"
    relationship: "Topic 19 test-CI duality informs §24.6's framing of Yang: selection consistency is an asymptotic claim about $\\mathbf{1}\\{\\hat{\\mathcal{M}} = \\mathcal{M}_*\\}$; prediction efficiency is a claim about $L^2$ risk — different parameters, different asymptotics, no compatibility guarantee."
  - topic: "exponential-families"
    relationship: "Topic 7 Thm 3 ($A'(\\theta) = \\mu$) and exponential-family log-likelihood structure are cited in §24.3 Proof 1. §24.4 Laplace uses observed Fisher information — for exponential families this is $A''(\\hat\\theta)$."
references:
  - type: "article"
    title: "A new look at the statistical model identification"
    author: "Hirotugu Akaike"
    year: 1974
    note: "AKA1974. Introduces AIC. §24.3 Thm 1 restates with modern notation."
  - type: "article"
    title: "Estimating the dimension of a model"
    author: "Gideon Schwarz"
    year: 1978
    note: "SCH1978. Four-page paper introducing BIC. §24.4 Proof 2 generalizes via Laplace approximation."
  - type: "article"
    title: "Some comments on Cp"
    author: "C. L. Mallows"
    year: 1973
    note: "MAL1973. Mallows' Cp for variable subset selection. §24.2 Def 3 + Ex 2."
  - type: "article"
    title: "An asymptotic equivalence of choice of model by cross-validation and Akaike's criterion"
    author: "M. Stone"
    year: 1977
    note: "STO1977. LOO-CV ≡ AIC under Gaussian-linear errors. §24.5 Thm 4 + Proof 3."
  - type: "article"
    title: "Can the strengths of AIC and BIC be shared? A conflict between model identification and regression estimation"
    author: "Yuhong Yang"
    year: 2005
    note: "YAN2005. Incompatibility theorem. §24.6 Thm 5; proof outside scope."
  - type: "book"
    title: "Model Selection and Multimodel Inference: A Practical Information-Theoretic Approach"
    author: "Kenneth P. Burnham & David R. Anderson"
    year: 2002
    edition: "2nd"
    publisher: "Springer"
    note: "BUR2002. Most widely cited applied reference on AIC. §24.3 Rem 5 AICc; §24.4 Rem 7 Akaike weights."
  - type: "book"
    title: "Model Selection and Model Averaging"
    author: "Gerda Claeskens & Nils Lid Hjort"
    year: 2008
    edition: "1st"
    publisher: "Cambridge University Press"
    note: "CLA2008. Canonical modern textbook. Ch. 2 AIC; Ch. 3 BIC; Ch. 4 model averaging."
  - type: "book"
    title: "The Elements of Statistical Learning: Data Mining, Inference, and Prediction"
    author: "Trevor Hastie, Robert Tibshirani & Jerome Friedman"
    year: 2009
    edition: "2nd"
    publisher: "Springer"
    note: "Inherited. §7.5 (AIC/BIC), §7.7 (BIC consistency), §7.10 (CV)."
  - type: "book"
    title: "Testing Statistical Hypotheses"
    author: "Erich L. Lehmann & Joseph P. Romano"
    year: 2005
    edition: "3rd"
    publisher: "Springer"
    note: "Inherited. §13.5 nested-model LRT — §24.7 Thm 6 identifies with AIC."
  - type: "book"
    title: "Statistical Inference"
    author: "George Casella & Roger L. Berger"
    year: 2002
    edition: "2nd"
    publisher: "Duxbury"
    note: "Inherited. §10.4 asymptotic efficiency; §24.3 Rem 6."
  - type: "book"
    title: "High-Dimensional Statistics: A Non-Asymptotic Viewpoint"
    author: "Martin J. Wainwright"
    year: 2019
    edition: "1st"
    publisher: "Cambridge University Press"
    note: "Inherited. Ch. 7 RE conditions; Ch. 11 post-selection. §24.10 Rem 20 + Rem 25."
---

import DefinitionBlock from '../../components/ui/DefinitionBlock.astro';
import TheoremBlock from '../../components/ui/TheoremBlock.astro';
import ProofBlock from '../../components/ui/ProofBlock.astro';
import ExampleBlock from '../../components/ui/ExampleBlock.astro';
import RemarkBlock from '../../components/ui/RemarkBlock.astro';
import ExternalLink from '../../components/ui/ExternalLink.astro';

{/*
  Component embeds (Task 9 — components not yet created; uncomment per section
  when each ships):

  import ICSelector from '../../components/viz/ICSelector.tsx';
  import CVvsICComparator from '../../components/viz/CVvsICComparator.tsx';
  import ConsistencyEfficiencyRace from '../../components/viz/ConsistencyEfficiencyRace.tsx';
*/}

## 24.1 The model-selection problem

{/* Section opening — TODO Task 7 prose: motivation paragraph on in-sample vs
    out-of-sample fit, the running sin(2πx) example, and why "smaller training
    loss" is not enough. */}

Given data $\mathbf{y}$ and a candidate family $\{\mathcal{M}_1, \mathcal{M}_2, \ldots, \mathcal{M}_M\}$ of statistical models — each with parameter space $\Theta_k$ and likelihood $L_k(\theta_k; \mathbf{y})$ — the **model-selection problem** is to choose one model from the family by a procedure that has a defensible asymptotic justification. Maximum log-likelihood $\hat\ell$ alone is not such a procedure: $\hat\ell$ is monotone in model complexity, so it always picks the largest candidate.

<DefinitionBlock number={1} title="Prediction risk">
For an estimator $\hat\theta$ trained on $\mathbf{y} = (Y_1, \ldots, Y_n)$ and a fresh observation $Y_{\text{new}} \sim f_0$ from the same data-generating distribution, the **prediction risk** under loss $L$ is

$$R(\hat\theta) \;:=\; \mathbb{E}_{Y_{\text{new}}}\!\left[L(\hat\theta;\, Y_{\text{new}})\right].$$

For log-likelihood loss $L(\hat\theta; y) = -\log f(y; \hat\theta)$, the risk is the negative expected log-predictive

$$R(\hat\theta) \;=\; -\mathbb{E}_{Y_{\text{new}}}\!\left[\log f(Y_{\text{new}}; \hat\theta)\right] \;=\; -\mathrm{EL}(\hat\theta).$$
</DefinitionBlock>

<DefinitionBlock number={2} title="Optimism gap">
The **training empirical risk** is $\hat R(\hat\theta) = (1/n)\sum_{i=1}^n L(\hat\theta; Y_i)$. The **optimism gap** measures how much in-sample loss underestimates out-of-sample loss in expectation:

$$\mathrm{opt} \;:=\; \mathbb{E}\!\left[R(\hat\theta)\right] - \mathbb{E}\!\left[\hat R(\hat\theta)\right].$$

For correctly-specified parametric models satisfying Topic 14's regularity conditions, $\mathrm{opt} = k/n + o(1/n)$ where $k = \dim(\theta)$ — the optimism is exactly the parameter count divided by sample size, to leading order.
</DefinitionBlock>

<ExampleBlock number={1} title="Polynomial fit on sin(2πx): in-sample lies, out-of-sample doesn't">
Generate $n=80$ observations from $y = \sin(2\pi x) + \mathcal{N}(0, 0.25^2)$ with $x \sim \mathcal{U}(0, 1)$. Fit polynomials of degree $d = 0, 1, \ldots, 12$ by ordinary least squares. The training $\mathrm{RSS}$ is monotone-decreasing in $d$ (every extra coefficient can only reduce the in-sample residual sum). The out-of-sample prediction risk, however, follows a U-shape: it falls as $d$ rises from $0$ to $\sim 6$ (model approximates the sine well), then climbs as $d$ exceeds $\sim 8$ (the polynomial wiggles to fit noise). Figure 1 shows the gap.
</ExampleBlock>

<img src="/images/topics/model-selection-and-information-criteria/24-optimism.png"
     alt="Two-panel figure. Left: training RSS vs polynomial degree d on the sin DGP, monotone-decreasing from d=0 to d=12. Right: out-of-sample prediction risk on a fresh test set of size 1000, U-shaped with minimum near d=6. Vertical reference line at d=6 in both panels. Title: 'In-sample fit lies; out-of-sample risk does not'."
     class="w-full rounded-lg border my-6"
     style="border-color: var(--color-border);" />

<RemarkBlock number={1} title="Selection as a decision problem">
{/* TODO Task 7: 2–3 sentences on why model selection is a *decision* over $\mathcal{M}$,
    contrasting with parameter estimation within a single model. Frame the AIC/BIC/CV
    triple as three different loss functions on the model space. */}
Model selection is a decision problem over the candidate set $\{\mathcal{M}_1, \ldots, \mathcal{M}_M\}$ — distinct from parameter estimation, which decides over $\Theta_k$ for a fixed $\mathcal{M}_k$. AIC, BIC, and cross-validation correspond to three different loss functions on the model space, each with its own asymptotic guarantee.
</RemarkBlock>

<RemarkBlock number={2} title="Three question families: consistency, efficiency, sparsity recovery">
{/* TODO Task 7: 4–5 sentences distinguishing (1) selection consistency
    (BIC's target), (2) prediction efficiency (AIC's target), (3) sparsity
    recovery (Topic 23 lasso's target). Stress that each is a different
    asymptotic regime and the procedures align with their respective targets. */}
Three asymptotic targets organize the literature: **selection consistency** ($\mathbb{P}(\hat{\mathcal{M}} = \mathcal{M}_*) \to 1$ when the truth is in the candidate set — BIC's target), **prediction efficiency** (minimax-rate optimal $L^2$ risk in the misspecified-nonparametric regime — AIC and CV's target), and **sparsity recovery** (correctly identifying $\mathrm{supp}(\boldsymbol\beta_*)$ — Topic 23's lasso target). §24.6 Thm 5 shows the first two are formally incompatible.
</RemarkBlock>

<a id="section-24-2"></a>

## 24.2 Mallows' $C_p$ — the Gaussian-linear predecessor

{/* Section opening — TODO Task 7 prose: 2–3 sentences placing Cp historically
    (Mallows 1973, eight years before Akaike's 1974 AIC paper, eleven years
    before Schwarz 1978's BIC). The Gaussian-linear setting is the simplest
    place to see the optimism-correction logic that AIC will generalize. */}

Before Akaike's information-theoretic framework, Mallows (1973) proposed a Gaussian-linear-specific criterion that estimates expected scaled prediction risk via a complexity penalty calibrated against a reference variance.

<DefinitionBlock number={3} title="Mallows' $C_p$">
For an OLS fit of a candidate model with $k$ free parameters (intercept + slopes + $\sigma^2$) to $n$ observations under the Gaussian linear model $\mathbf{y} = \mathbf{X}\boldsymbol\beta + \mathcal{N}(\mathbf{0}, \sigma^2\mathbf{I})$, with residual sum of squares $\mathrm{RSS}_k$, **Mallows' $C_p$** is

$$C_p \;:=\; \frac{\mathrm{RSS}_k}{\hat\sigma^2_{\text{ref}}} + 2k - n,$$

where $\hat\sigma^2_{\text{ref}}$ is the MLE error variance from the **largest** candidate model (the conventional reference).
</DefinitionBlock>

<ExampleBlock number={2} title="$C_p$ unbiasedness for scaled prediction risk (embedded derivation)">
Under the Gaussian linear model with $\hat\sigma^2_{\text{ref}}$ known (or treated as known via the reference convention), the expected scaled in-sample residual sum is

$$\mathbb{E}\!\left[\mathrm{RSS}_k / \sigma^2\right] \;=\; n - k.$$

Adding $2k - n$ to both sides:

$$\mathbb{E}[C_p] \;=\; (n - k) + 2k - n \;=\; k.$$

A separate calculation (Mallows 1973 §3) gives the expected scaled prediction risk on a fresh test set of size $n$:

$$\mathbb{E}\!\left[\mathrm{R}_{\text{scaled}}\right] \;=\; k + (\text{model bias term}).$$

Under correct specification (model bias $= 0$), $\mathbb{E}[C_p] = \mathbb{E}[\mathrm{R}_{\text{scaled}}] = k$. Otherwise $C_p$ overestimates the parameter count by exactly the model bias — making $C_p$ minus its parameter count a calibrated estimator of model bias, the original use case Mallows 1973 §3 emphasized.
</ExampleBlock>

<RemarkBlock number={3} title="$C_p$ ≡ AIC under Gaussian-linear errors">
{/* TODO Task 7: 3–4 sentences showing the $C_p$/AIC equivalence. Both estimate
    the same expected scaled prediction risk to leading order; AIC generalizes
    away from Gaussian. */}
Under the Gaussian linear model, $\mathrm{AIC} = n\log(\mathrm{RSS}/n) + 2k$ (dropping additive constants), and a Taylor expansion of $\log(\mathrm{RSS}/n)$ around $\sigma^2_{\text{ref}}$ recovers $C_p$ up to higher-order terms. The two criteria rank candidates identically under Gaussian-homoscedastic errors; AIC is the strict generalization to other exponential families (§24.3 Thm 1).
</RemarkBlock>

<RemarkBlock number={4} title="Choice of $\hat\sigma^2_{\text{ref}}$">
{/* TODO Task 7: 4–5 sentences on the practical question of which model gives
    the reference variance. Mallows recommended the largest candidate; HAS2009
    §7.5 discusses the bias if the largest is itself misspecified. */}
The convention $\hat\sigma^2_{\text{ref}} = \mathrm{RSS}_{\text{full}}/(n - k_{\text{full}})$ — using the unbiased estimator from the largest candidate — has the cleanest theory: $\hat\sigma^2_{\text{ref}}$ is unbiased for $\sigma^2$ if the largest model is correctly specified, and $C_p$'s argmin then targets the bias-variance-optimal submodel. T10.8 in `regression.test.ts` pins $\hat\sigma^2_{\text{ref}} = 0.0497586381$ for the canonical POLY_DGP at $n=80$, giving $\arg\min_d C_p = 6$.
</RemarkBlock>

<ExampleBlock number={3} title="$C_p$ on the polynomial DGP (T10 pinned)">
On the canonical POLY_DGP ($n=80$, $\sigma=0.25$, seed $42$), with $\hat\sigma^2_{\text{ref}} = 0.0497586381$ from the $d = 12$ fit, the Mallows $C_p$ values for $d = 0, 1, \ldots, 12$ have argmin at **$d = 6$ with $C_p = 21.4569$** (T10.8). The argmin coincides with AIC's argmin (T10.3), illustrating Rem 3's $C_p \equiv \mathrm{AIC}$ equivalence on the Gaussian linear model. Figure 2 plots the $C_p$ curve alongside the in-sample $\mathrm{RSS}/\hat\sigma^2_{\text{ref}}$ to make the $+2k - n$ correction visually concrete.
</ExampleBlock>

<img src="/images/topics/model-selection-and-information-criteria/24-cp-polynomial.png"
     alt="Two curves over polynomial degree d=0..12 on the POLY_DGP. Bottom curve: scaled RSS (RSS/sigma^2_ref) — monotone-decreasing. Top curve: Mallows Cp = scaled RSS + 2k - n — U-shaped with argmin at d=6. Vertical reference at argmin Cp. Title: 'Mallows Cp adds the +2k - n optimism correction'."
     class="w-full rounded-lg border my-6"
     style="border-color: var(--color-border);" />

<a id="section-24-3"></a>

## 24.3 Akaike's information criterion (FEATURED)

{/* Section opening — TODO Task 7 prose: framing paragraph on Akaike's 1974
    information-theoretic insight. The optimism k/n result of §24.1 Def 2
    becomes the +2k AIC penalty after multiplying by the -2 log-likelihood
    convention. Proof 1 is the load-bearing technical content. */}

Akaike (1974) showed that the optimism gap of Def 2 is exactly $k/n$ to leading order under correct specification + regularity, and that $-2\hat\ell + 2k$ is therefore an asymptotically unbiased estimator of $-2n$ times the expected out-of-sample log-likelihood. This is **AIC** — and the heart of the modern model-selection framework.

<DefinitionBlock number={4} title="KL divergence and expected log-likelihood">
For two densities $f_0$ (truth) and $f(\cdot;\theta)$ (model), the **Kullback–Leibler divergence** is

$$D(f_0 \,\|\, f_\theta) \;:=\; \mathbb{E}_{Y \sim f_0}\!\left[\log\frac{f_0(Y)}{f(Y;\theta)}\right] \;=\; \mathbb{E}_{f_0}[\log f_0(Y)] - \mathrm{EL}(\theta),$$

where the **expected log-likelihood** under truth is

$$\mathrm{EL}(\theta) \;:=\; \mathbb{E}_{Y \sim f_0}[\log f(Y;\theta)].$$

Since $\mathbb{E}_{f_0}[\log f_0(Y)]$ does not depend on $\theta$, **maximizing $\mathrm{EL}(\theta)$ is equivalent to minimizing $D(f_0 \,\|\, f_\theta)$**.
</DefinitionBlock>

<TheoremBlock number={1} title="AIC bias-correction (Akaike 1974)">
Let $Y_1, \ldots, Y_n$ be iid from unknown density $f_0$, and let $\{f(\cdot;\theta) : \theta \in \Theta \subset \mathbb{R}^k\}$ be a parametric model satisfying Topic 14's regularity conditions: smooth log-likelihood, positive-definite Fisher information, MLE asymptotic normality. Let $\hat\theta$ be the MLE, $\hat\ell = \ell(\hat\theta;\mathbf{y})$, and define $\mathrm{AIC} := -2\hat\ell + 2k$. Then under correct specification ($f_0 \in \{f(\cdot;\theta)\}$),

$$\mathbb{E}\!\left[\mathrm{AIC}\right] \;=\; -2n\cdot\mathbb{E}\!\left[\mathrm{EL}(\hat\theta)\right] + o(1).$$

That is, $\mathrm{AIC}$ is asymptotically unbiased for $-2n$ times the expected out-of-sample log-predictive evaluated at the MLE.
</TheoremBlock>

<ProofBlock>

**Setup.** Let $Y_1, \dots, Y_n$ be iid from unknown density $f_0$; let $\{f(\cdot;\theta) : \theta \in \Theta \subset \mathbb{R}^k\}$ be a parametric model satisfying Topic 14's regularity conditions (smooth $\ell$, positive-definite Fisher information, MLE asymptotic normality). We want to rank candidate models by expected out-of-sample log-predictive

$$\mathrm{EL}(\theta) := \mathbb{E}_{Y_{\text{new}} \sim f_0}[\log f(Y_{\text{new}}; \theta)],$$

evaluated at the plug-in estimator $\hat\theta = \hat\theta(Y_1, \dots, Y_n)$. The target is $\mathbb{E}[\mathrm{EL}(\hat\theta)]$; the naive estimator is $\hat\ell/n$. We show the gap is $k/n$ to leading order.

**Step 1 — KL-projected parameter.** Define

$$\theta_0 := \arg\min_{\theta \in \Theta} D(f_0 \,\|\, f(\cdot;\theta)) = \arg\max_{\theta \in \Theta} \mathrm{EL}(\theta).$$

Under correct specification, $f_0 = f(\cdot; \theta_0)$. Under misspecification, $\theta_0$ is the best-in-family parameter.

**Step 2 — MLE convergence.** Under regularity,

$$\sqrt{n}(\hat\theta - \theta_0) \;\xrightarrow{d}\; \mathcal{N}(\mathbf{0}, \;\mathcal{K}(\theta_0)^{-1} \mathcal{J}(\theta_0) \mathcal{K}(\theta_0)^{-1}),$$

where $\mathcal{J} = \operatorname{Var}_{f_0}[\nabla\log f]$ and $\mathcal{K} = -\mathbb{E}_{f_0}[\nabla^2\log f]$. Under correct specification, $\mathcal{J} = \mathcal{K} =: \mathcal{I}$ (Fisher identity, Topic 14 Thm 3), and the sandwich collapses to $\mathcal{I}^{-1}$.

**Step 3 — Taylor-expand $\hat\ell(\hat\theta)$ around $\theta_0$.**

$$\hat\ell(\hat\theta) = \hat\ell(\theta_0) + (\hat\theta - \theta_0)^\top \nabla\hat\ell(\theta_0) + \tfrac12 (\hat\theta - \theta_0)^\top \nabla^2\hat\ell(\theta_0)(\hat\theta - \theta_0) + o_p(1).$$

The score $\nabla\hat\ell(\theta_0) = \sum_i \nabla\log f(Y_i;\theta_0)$ has mean zero under $f_0$ at $\theta_0$ (stationarity of $\mathrm{EL}$). By LLN, $n^{-1}\nabla^2\hat\ell(\theta_0) \to -\mathcal{K}(\theta_0)$.

**Step 4 — Taylor-expand $\mathrm{EL}(\hat\theta)$ around $\theta_0$.**

$$\mathrm{EL}(\hat\theta) = \mathrm{EL}(\theta_0) - \tfrac12 (\hat\theta - \theta_0)^\top \mathcal{K}(\theta_0)(\hat\theta - \theta_0) + o_p(1).$$

First-order vanishes (stationarity of $\mathrm{EL}$); second-order negative since $\mathcal{K}$ is positive-definite.

**Step 5 — Take expectations under $f_0^n$.** Using Step 3 + asymptotic covariance:

$$\mathbb{E}[\hat\ell(\hat\theta)] = n\cdot\mathrm{EL}(\theta_0) + \tfrac12\mathbb{E}\!\left[(\hat\theta - \theta_0)^\top \nabla^2\hat\ell(\theta_0)(\hat\theta - \theta_0)\right] + o(1).$$

With $n^{-1}\nabla^2\hat\ell(\theta_0) \to -\mathcal{K}(\theta_0)$ and $\operatorname{Cov}(\sqrt n(\hat\theta - \theta_0)) \to \mathcal{K}^{-1}\mathcal{J}\mathcal{K}^{-1}$:

$$\mathbb{E}[\hat\ell(\hat\theta)] = n\cdot\mathrm{EL}(\theta_0) - \tfrac12\operatorname{tr}(\mathcal{K}^{-1}\mathcal{J}) + o(1).$$

Similarly from Step 4:

$$\mathbb{E}[\mathrm{EL}(\hat\theta)] = \mathrm{EL}(\theta_0) - \tfrac{1}{2n}\operatorname{tr}(\mathcal{K}^{-1}\mathcal{J}) + o(1/n).$$

**Step 6 — Combine.** Multiply the $\mathrm{EL}$ expression by $n$, subtract:

$$\mathbb{E}[\hat\ell(\hat\theta)] - n\cdot\mathbb{E}[\mathrm{EL}(\hat\theta)] = \operatorname{tr}(\mathcal{K}^{-1}\mathcal{J}) + o(1).$$

**Step 7 — Specialize.** Under correct specification, $\mathcal{J} = \mathcal{K}$, so $\operatorname{tr}(\mathcal{K}^{-1}\mathcal{J}) = \operatorname{tr}(\mathbf{I}_k) = k$. Multiplying by $-2$:

$$-2\mathbb{E}[\hat\ell] + 2k = -2n\cdot\mathbb{E}[\mathrm{EL}(\hat\theta)] + o(1).$$

The left side is $\mathbb{E}[\mathrm{AIC}]$. The right side is $-2n$ times the expected log-predictive — what we wanted to estimate. AIC is asymptotically unbiased for $-2n\cdot\mathbb{E}[\mathrm{EL}(\hat\theta)]$ under correct specification.

Under misspecification ($\mathcal{J} \neq \mathcal{K}$), the correct penalty is $2\operatorname{tr}(\hat{\mathcal{K}}^{-1}\hat{\mathcal{J}})$: Takeuchi's TIC (Rem 6). ∎ — using Topic 14 Thm 6 (MLE asymptotic normality), Topic 14 Thm 3 (Fisher identity), multivariate delta method (Topic 6).

</ProofBlock>

<ExampleBlock number={4} title="AIC on the polynomial DGP (T10 pinned)">
On POLY_DGP ($n=80$, $\sigma=0.25$, seed $42$), $\mathrm{AIC}(d)$ values for $d = 0, 1, \ldots, 12$ have argmin at **$d = 6$ with $\mathrm{AIC} = 8.2633$** (T10.3). The $\mathrm{AIC}$ curve is U-shaped: $\mathrm{AIC}(d=0) = 179.0128$ (extreme underfit, T10.1); $\mathrm{AIC}(d=3) = 13.5077$ (undershooting; T10.2); $\mathrm{AIC}(d=6) = 8.2633$ (argmin, recovers the prediction-risk argmin from §24.1 Ex 1); $\mathrm{AIC}(d=12) = 14.9845$ (overfit, T10.4). Figure 3 overlays AIC and the bias-correction term $+2k$ to separate the empirical log-likelihood from the optimism penalty.
</ExampleBlock>

<img src="/images/topics/model-selection-and-information-criteria/24-aic-bias-correction.png"
     alt="Three-curve plot over polynomial degree d=0..12 on POLY_DGP. Curve A: -2 log-likelihood, monotone-decreasing. Curve B: +2k bias-correction penalty, linear in d. Curve C: AIC = sum of A and B, U-shaped with argmin at d=6 (vertical reference). Title: 'AIC = -2 log-lik + 2k separates fit from complexity'."
     class="w-full rounded-lg border my-6"
     style="border-color: var(--color-border);" />

<RemarkBlock number={5} title="Corrected AIC for small samples (AICc)">
{/* TODO Task 7: 4–5 sentences with the full Hurvich–Tsai 1989 / Sugiura 1978
    derivation summary, the formula AICc = AIC + 2k(k+1)/(n-k-1), the regime
    where it matters (n / k < ~40), and the BUR2002 §2.4 default-to-AICc
    recommendation. */}
**AICc** corrects AIC's small-sample bias when $n/k$ is not large (Hurvich & Tsai 1989; Sugiura 1978):

$$\mathrm{AICc} \;=\; \mathrm{AIC} + \frac{2k(k+1)}{n - k - 1}.$$

Exact for Gaussian-linear models; asymptotic correction otherwise. The penalty $2k(k+1)/(n-k-1) \to 0$ as $n \to \infty$, so AICc and AIC agree asymptotically. Burnham & Anderson 2002 §2.4 recommend AICc as the default whenever $n/k < 40$.
</RemarkBlock>

<RemarkBlock number={6} title="Takeuchi information criterion (TIC) and information geometry">
{/* TODO Task 7: 3–4 sentences on TIC as the misspecification-robust generalization,
    the formula -2ℓ + 2 tr(K^{-1} J), why nobody uses it in practice (J and K hard
    to estimate stably), and a one-sentence pointer to formalml's information
    geometry topic. */}
Takeuchi (1976) generalized AIC to misspecified parametric models:

$$\mathrm{TIC} \;=\; -2\hat\ell + 2\operatorname{tr}(\hat{\mathcal{K}}^{-1}\hat{\mathcal{J}}),$$

with $\hat{\mathcal{J}}$ the empirical score outer product and $\hat{\mathcal{K}}$ the observed Hessian of $-\ell$. Under correct specification $\hat{\mathcal{K}}^{-1}\hat{\mathcal{J}} \to \mathbf{I}_k$ and TIC $\to$ AIC. TIC sees little practical use because $\hat{\mathcal{J}}$ and $\hat{\mathcal{K}}$ are hard to estimate stably at moderate $n$; the broader information-geometric framing lives at formalml.
</RemarkBlock>

{/* Component embed (Task 9 — uncomment when ICSelector ships):
<ICSelector client:visible />
*/}

<a id="section-24-4"></a>

## 24.4 Schwarz's BIC and the Bayesian bridge

{/* TODO Task 7: §24.4 prose (~1700 words, LONGEST) — Def 5 (BIC), Thm 2 + Proof 2
    (4 steps, verbatim), Thm 3 (BIC consistency, stated), Ex 5 + Fig 4
    (24-bic-laplace.png), Rems 7–10 (Track-7 amplification per Appendix B #15:
    Rem 7 BMA flag, Rem 8 prior on model space, Rem 10 marginal likelihood
    computation). */}

<a id="section-24-5"></a>

## 24.5 Stone's CV ≡ AIC equivalence

{/* TODO Task 7: §24.5 prose (~1200 words) — Def 6 (LOO-CV), Thm 4 + Proof 3
    (5 steps, verbatim), Ex 6 + Fig 5 (24-stone-equivalence.png), Rems 11–13
    (Rem 11 nested CV — discharges Topic 23 R1), <CVvsICComparator client:visible />
    embed. */}

<a id="section-24-6"></a>

## 24.6 Yang's incompatibility theorem

{/* TODO Task 7: §24.6 prose (~900 words) — Thm 5 (stated only, proof outside
    scope), Ex 7 + Fig 6 (24-yang-race.png), Rems 14–16,
    <ConsistencyEfficiencyRace client:visible /> embed. */}

<a id="section-24-7"></a>

## 24.7 Nested-model selection: AIC ≡ LRT with default threshold

{/* TODO Task 7: §24.7 prose (~700 words) — Thm 6 embedded derivation (4 lines:
    ΔAIC = 2q - 2Δℓ̂ via -2ℓ + 2k), Ex 8 + Fig 7 (24-nested-glm.png) using
    pinned T10.12–T10.19 values, Rems 17–18. */}

<a id="section-24-8"></a>

## 24.8 Effective degrees of freedom for penalized estimators

{/* TODO Task 7: §24.8 prose (~800 words) — Def 7 (effective DOF),
    Thm 7 embedded SVD derivation (ridge), Thm 8 stated (lasso, Tibshirani–Taylor 2012),
    Ex 9 (T10.20–T10.25 ridge DOF table), Ex 10 + Fig 8 (24-lasso-aic-path.png)
    using PROSTATE_CANCER_DATA, Rems 19–20. */}

<a id="section-24-9"></a>

## 24.9 Worked examples

{/* TODO Task 7: §24.9 prose (~1100 words) — Exs 11–13 spanning the three
    workflows (polynomial sin DGP, nested Poisson, prostate lasso path with
    AIC overlay), Rems 21–22. */}

<a id="section-24-10"></a>

## 24.10 Forward map

{/* TODO Task 7: §24.10 prose (~800 words) — Rems 19–27 forward-pointing to
    Track 7 (BMA, MCMC, DIC/WAIC), Track 8 (nonparametric selection), formalml
    (post-selection inference, debiased lasso, cross-fitting, SRM/VC), with
    closing paragraph that reciprocally frames Topic 23 as a special case
    of Topic 24's framework. Fig 9 (24-forward-map.png). */}

---

### References

1. Akaike, H. (1974). [*A new look at the statistical model identification*](https://doi.org/10.1109/TAC.1974.1100705). *IEEE Transactions on Automatic Control*, 19(6), 716–723.
2. Schwarz, G. (1978). [*Estimating the dimension of a model*](https://doi.org/10.1214/aos/1176344136). *The Annals of Statistics*, 6(2), 461–464.
3. Mallows, C. L. (1973). [*Some comments on Cp*](https://doi.org/10.1080/00401706.1973.10489103). *Technometrics*, 15(4), 661–675.
4. Stone, M. (1977). [*An asymptotic equivalence of choice of model by cross-validation and Akaike's criterion*](https://doi.org/10.1111/j.2517-6161.1977.tb01603.x). *Journal of the Royal Statistical Society, Series B*, 39(1), 44–47.
5. Yang, Y. (2005). [*Can the strengths of AIC and BIC be shared? A conflict between model identification and regression estimation*](https://doi.org/10.1093/biomet/92.4.937). *Biometrika*, 92(4), 937–950.
6. Burnham, K. P. & Anderson, D. R. (2002). [*Model Selection and Multimodel Inference: A Practical Information-Theoretic Approach*](https://doi.org/10.1007/b97636) (2nd ed.). Springer.
7. Claeskens, G. & Hjort, N. L. (2008). [*Model Selection and Model Averaging*](https://doi.org/10.1017/CBO9780511790485) (1st ed.). Cambridge University Press.
8. Hastie, T., Tibshirani, R. & Friedman, J. (2009). [*The Elements of Statistical Learning*](https://hastie.su.domains/ElemStatLearn/) (2nd ed.). Springer.
9. Lehmann, E. L. & Romano, J. P. (2005). [*Testing Statistical Hypotheses*](https://link.springer.com/book/10.1007/0-387-27605-X) (3rd ed.). Springer.
10. Casella, G. & Berger, R. L. (2002). [*Statistical Inference*](https://www.routledge.com/Statistical-Inference/Casella-Berger/p/book/9780534243128) (2nd ed.). Duxbury.
11. Wainwright, M. J. (2019). [*High-Dimensional Statistics: A Non-Asymptotic Viewpoint*](https://doi.org/10.1017/9781108627771) (1st ed.). Cambridge University Press.
